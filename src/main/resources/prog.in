programa teste_rust
inicio
    declare a, b: inteiros.
    let c: inteiro.
    
    a := 3.
    b := 2.
    c := a + b * 5.

    escreva(a).
    escreva(b).
    escreva(c).

    fn minha_funcao(x: inteiro) -> inteiro
    inicio
        let y: inteiro.
        y := x * 2.
        retorno y.
    fim

    let resultado: inteiro.
    resultado := minha_funcao(c).
    escreva(resultado).
fim